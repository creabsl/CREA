# Development - use current window location (works for both localhost and production)
# Setting to empty string will use production URL as fallback
# For localhost, we need to explicitly set it
VITE_API_URL=http://localhost:5000